-- explanation:
-- tracks how many users reach each funnel stage
-- helps identify major drop-off points

select
    funnel_stage,
    count(distinct user_id) as users_count
from funnel_processed
group by funnel_stage
order by users_count desc;
